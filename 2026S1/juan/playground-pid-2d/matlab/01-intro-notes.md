# Intro Notes

This folder contains a compact MATLAB playground for a drone-control project.

The implementation is intentionally simplified:

- `1D` means vertical motion with thrust opposing gravity
- `2D` means planar motion with horizontal position, altitude, and pitch
- PID controllers are cascaded in the 2D case

Use these scripts to test intuition before moving to the Python simulator.
